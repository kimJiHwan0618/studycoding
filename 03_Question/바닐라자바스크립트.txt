리액트 시작전 기초 바닐라 자바스크립트 개념 정리

- function
 - 함수 선언식
   - 변수 선언이 var로 시작되는것처럼, 함수 선언은 function 으로 시작된다.
     선언 된 함수는 나중 사용을 위해 저장되며, call 될 때 실행된다.

 - 함수 표현식
   - 변수에 저장되는 함수 형식. 함수는 변수 이름으로 호출될 수 있으며, 
     변수가 정의되기전 호출할 수 없다.

https://velog.io/@bisu8018/%ED%95%A8%EC%88%98-%ED%91%9C%ED%98%84%EC%8B%9D-VS-%ED%95%A8%EC%88%98-%EC%84%A0%EC%96%B8%EC%8B%9D

- 클로저
  {내부함수, lexical 스코프, 클로저의 기억}
  - 프로그래밍 용어
  설명 정리
  1. 클로저는 어떤 함수가 자신의 내부가 아닌 외부에서 선언된 변수에 접근하는것을 뜻함.
  2. 외부 변수를 기억하고 이 외부 변수에 접근할 수 있는 함수를 의미
  3. 함수 안에서 선언된 내부함수.
https://ko.javascript.info/closure
[[Environment]] 프로퍼티와 렉시컬 환경이 어떤 방식으로 동작하는지

- 호이스팅
  변수를 선언하고 초기화했을 때 선언 부분이 
  최상단으로 끌어올려지는 현상을 의미합니다

- 스코프
  - 중괄호로 둘러쌓인 코드의 영역 
  ex ) if() { 스코프 } for() { 스코프 } function() { 스코프 } 
  - 특정 스코프 안에서 선언된 변수는 스코프 바깥에서 사용할 수 없습니다.

- context 
  문맥, 혹은 코드의 실행 환경.
  코드의 실행순서를 정해줌. 

-return
함수 실행을 종료하고, 주어진 값을 함수 호출 지점으로 반환합니다.

-argument
 -- 인자값 또는 매개변수

- let 

- const 
선언과 동시에 값을 대입해줘야 한다.

- jsx
  js를 확장한 문법. react에서 많이 쓰이며, html을 렌더링 하는 언어이면서 
  자바스크립트와 함께쓸수있습니다.

- javascript 에서 this란? 
  호출에 따라 달라진다
  - 단독으로 쓰는경우 window 객체를 가르킵니다.
  - 메서드안 : 해당 메서드를 가르킵니다.
  - 이벤트 핸들러안 : 이벤트를 받는 html 요소
   


동기 
 - 동기식은 동시에 일어나는 뜻이다. 요청과 그 결과가 동시에 일어난다는 말!

   장점 : 설계가 간단하고 직관적
   단점 : 결과를 볼 때까지 아무것도 못하고 대기해야함

비동기 
  - 비동기식은 동시에 일어나지 않는다는 뜻이다. 요청과 결과가 동시에 일어나지 않는다는 말!

    장점 : 결과가 주어지는데 시간이 걸리더라도 그 시간 동안 다른 작업이 가능해 자원의 효율적인 사용이 가능
    단점 : 설계가 동기보다 복잡함

babel
  - 자바스크립트 컴파일러 
   모든 자바스크립트 언어를 모든 브라우저에서 실행시켜줄수있도록 호환성을 지켜준다(jsx, ts, es5)

es6 (let, const, map, class)


- restful API
REST란, "웹에 존재하는 모든 자원(이미지, 동영상, DB 자원)에 고유한 
URI를 부여해 활용" 하는 것으로,  자원을 정의하고 
자원에 대한 주소를 지정하는 방법론을 의미한다고 한다.
따라서 Restful API는 REST 특징을 지키면서 API를 제공하는 것을 의미한다. 

- API(Application Programing Interface)
API는 응용 프로그램에서 사용할 수 있도록, 
운영 체제나 프로그래밍 언어가 제공하는 기능을 제어할 수 
있게 만든 인터페이스를 뜻한다.


- XML 과 JSON 의 차이점에 대해 설명해주세요.
  1. JSON이 XML 보다 데이터를 더 빨리 읽고 쓸수 있다. 
  2. XML은 배열을 사용할 수 없지만 JSON은 가능하다.
  3. JSON은 종료태그를 사용하지 않는다.

